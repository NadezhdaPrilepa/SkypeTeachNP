package skype.teach.np.calculator;

/**class of calculate input String Expression, contains simple math operators,
 * double number and parenthesis. If expression contains division on 0
 * return exception, else result of expression
 * @author NPrilepa
 */
public class NpCalculator implements INpCalculator {

    private String inputString;

    public NpCalculator(String inputString) {
        this.inputString = inputString;
    }

    public double calculate() {

        return 0;
    }
}
